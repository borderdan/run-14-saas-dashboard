# Environment Variables Configuration
# This is an example of the environment variables needed for the project.
# Copy this file to .env.local and populate with your own values.

# Currently, there are no referenced environment variables in the source code.
# When adding new ones, document them here (e.g. API keys, database URLs, etc.).

# Example:
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
